services:
  - type: worker
    name: pocket-signal-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python pocket_signal_bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false
